// img
const assets = "/assets/";
const img = `${assets}images/`;
const utils = `${assets}utils/`;
const lg = `${img}logo/`;
const icons = `${img}icons/`;
const partner = `${img}partner/`;
const about = `${img}about/`;
const doneWork = `${img}doneWork/`;
const article = `${img}article/`;

export const logo = `${lg}bom-white-text.png`;
export const logoMiniBlue = `${lg}bom-blue.png`;
export const logoMiniWhite = `${lg}bom-white.png`;

export class Assets {
    
    static logoMiniWhite = `${lg}bom-white.png`;
    static logoMiniBlue = `${lg}bom-blue.png`;
    static logoWhite = `${lg}bom-white-text.png`;
    static logoBlue = `${lg}bom-blue-text.png`;
    
    
    static staticWallet = `${icons}static/wallet.png`;
    static wallet = `${icons}wallet.gif`;
    
    static login = `${img}city1.jpg`
    
}

// svg
const svg = `${assets}svg/`;

// voice
const voice = `${assets}voice/`;

export const imgLogo = `${assets}logo.png`;
export const imgLogoWhite = `${assets}logo white.png`;
export const districtIcon = `${icons}district.png`
export const compareArrowIcon = `${icons}compareArrow.png`