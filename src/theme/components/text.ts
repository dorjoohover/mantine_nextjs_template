import { Title } from "@mantine/core";

export const text = {
  Title: Title.extend({
    defaultProps: {
      fw: 700,
      className: "uppercase",
      mx: "auto",
      c: "slateGrey",
      size: "xl",
    },
  }),
};
